# Property Management System - Environment Variables
# Copy this file to .env.local (development) or .env.production (production)

# ==============================================================================
# DATABASE
# ==============================================================================

# PostgreSQL connection string
# Development: postgresql://postgres:postgres@localhost:5432/propertymanagement
# Production:  postgresql://propman:YOUR_SECURE_PASSWORD@db:5432/propertymanagement
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/propertymanagement

# For docker-compose.prod.yml
DB_USER=propman
DB_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD
DB_NAME=propertymanagement

# ==============================================================================
# APPLICATION
# ==============================================================================

# Environment (development | production)
NODE_ENV=development

# Application URL (used in emails and OAuth callbacks)
# Development: http://localhost:3000
# Production:  https://your-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Log level (trace | debug | info | warn | error)
LOG_LEVEL=debug

# ==============================================================================
# SECURITY
# ==============================================================================

# Secret for authenticating cron job API calls
# Generate with: openssl rand -base64 32
CRON_SECRET=

# Secret for signing auth session cookies (HMAC-SHA256)
# MUST be at least 32 characters
# Generate with: openssl rand -base64 32
AUTH_SECRET=

# AES-256-GCM encryption key for Gmail OAuth tokens
# MUST be exactly 64 hex characters (32 bytes)
# Generate with: openssl rand -hex 32
TOKEN_ENCRYPTION_KEY=

# ==============================================================================
# GMAIL OAUTH (Required for email sync)
# ==============================================================================

# Google OAuth 2.0 credentials
# Create at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# OAuth callback URL (must match Google Cloud Console configuration)
# Development: http://localhost:3000/api/auth/gmail/callback
# Production:  https://your-domain.com/api/auth/gmail/callback
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/gmail/callback

# Gmail account for sending system notifications
NOTIFICATION_EMAIL=

# ==============================================================================
# PUSHOVER NOTIFICATIONS (Optional)
# ==============================================================================

# Pushover API token (create app at https://pushover.net/apps)
PUSHOVER_TOKEN=

# User keys for each recipient (found at https://pushover.net after login)
PUSHOVER_USER_ANNE=
PUSHOVER_USER_TODD=

# ==============================================================================
# AI FEATURES (Optional)
# ==============================================================================

# Anthropic API key for AI vendor matching
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# ==============================================================================
# PRODUCTION CHECKLIST
# ==============================================================================
# Before deploying to production, ensure:
# [ ] DATABASE_URL points to production database
# [ ] NODE_ENV=production
# [ ] NEXT_PUBLIC_APP_URL is your domain with HTTPS
# [ ] CRON_SECRET is set to a strong random value
# [ ] TOKEN_ENCRYPTION_KEY is set (64 hex chars)
# [ ] Google OAuth credentials are configured for production domain
# [ ] GOOGLE_REDIRECT_URI uses HTTPS
# [ ] NOTIFICATION_EMAIL is set
